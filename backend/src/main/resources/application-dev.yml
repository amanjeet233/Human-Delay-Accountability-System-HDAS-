spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hdas?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 'Amanjeet@4321.'
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  h2:
    console:
      enabled: false
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: false

  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql

server:
  port: 8080

logging:
  level:
    root: INFO
    com.hdas: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
  file:
    name: boot-dev.txt
    max-size: 10MB
    max-history: 30

feature:
  escalation:
    enabled: true
  audit-compliance:
    enabled: true
  advanced-accountability:
    enabled: true
  governance-analysis:
    enabled: true
  transparency:
    enabled: true